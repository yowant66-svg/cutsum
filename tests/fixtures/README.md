# Fixture policy

Only maintainer-authored text and locally generated synthetic media may enter
this directory. Every persisted fixture must record SPDX license, provenance,
generator version and SHA-256 in `manifest.json`.
